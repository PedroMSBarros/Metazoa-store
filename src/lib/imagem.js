// NOTA: postimg.cc bloqueia proxies de otimizacao (hotlink protection),
// entao por enquanto retornamos a URL original sem modificar.
// Ver conversa sobre migracao de hospedagem de imagens para resolver
// o problema de velocidade de forma definitiva.
export function otimizarImagem(url, largura = 500) {
  return url
}
